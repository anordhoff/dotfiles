# vim: ft=zsh
# if no arguments are passed, create two temporary files and diff them with vim

if [ $# -eq 0 ]; then
  tmp1=$(mktemp /tmp/vim.XXXXX)
  tmp2=$(mktemp /tmp/vim.XXXXX)
  set -- $tmp1 $tmp2
fi

if [[ $OSTYPE =~ "linux"* ]]; then
  gvimdiff -v $@
else
  command vimdiff $@
fi
