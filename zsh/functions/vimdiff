# vim: ft=zsh
# create two temporary files and diff them with vim

tmp1=$(mktemp /tmp/vim.XXXXX)
tmp2=$(mktemp /tmp/vim.XXXXX)

if [[ $OSTYPE =~ "linux"* ]]; then
  gvimdiff -v $tmp1 $tmp2
else
  command vimdiff $tmp1 $tmp2
fi
