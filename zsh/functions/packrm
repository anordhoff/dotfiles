# vim: ft=zsh

if [[ "${PWD}" != "${HOME}/dotfiles" ]]; then
	echo "packrm must me executed at ~/dotfiles"
else
	while [ -n "$1" ]; do
		git submodule deinit -f "nvim/package/start/${1}"
		rm -rf ".git/modules/nvim/package/start/${1}"
		git rm -f "nvim/package/start/${1}"
		shift
	done
fi
